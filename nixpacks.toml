[phases.setup]
nixPkgs = ["python311", "python311Packages.numpy", "python311Packages.pandas", "nodejs-18_x"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"